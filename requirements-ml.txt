# Keep aligned with pyproject.toml [project.optional-dependencies] ml.
# Optional ML/OCR direct dependencies ([project.optional-dependencies] ml)
# Install on a host that already has runtime deps: pip install -r requirements-ml.txt
# Full resolved stack (includes PyTorch): uv sync --locked --extra ml
numpy>=1.24
opencv-python-headless>=4.8
easyocr>=1.7

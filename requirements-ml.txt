# Optional OCR stack (EasyOCR + PyTorch). Install locally for plate recognition demos:
#   pip install -r requirements-ml.txt
#   uv sync --extra ml
#
# Not installed on Render free tier (512MB RAM). POST /api/v1/ml/recognize-plate returns 503 there.
easyocr>=1.7

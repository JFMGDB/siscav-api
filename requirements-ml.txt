# ML/OCR dependencies are now part of runtime requirements.txt (pyproject [project.dependencies]).
# This file remains for backward-compatible docs; use: pip install -r requirements.txt
-r requirements.txt

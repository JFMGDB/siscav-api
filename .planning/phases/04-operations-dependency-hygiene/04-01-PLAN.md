---
phase: 04-operations-dependency-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/api/v1/db/session.py
  - .planning/codebase/CONCERNS.md
  - docs/installation.md
  - docs/setup_database_guide.md
autonomous: true
requirements_addressed:
  - OPS-01
must_haves:
  truths:
    - App `session.py` has no `#region agent log`, no `create_all`, and no writes to `debug-0c9557.log`
    - First-time SQLite setup is documented as Alembic-driven (`alembic upgrade head`), not implicit table creation at import
    - CONCERNS no longer claims `config.py` contains agent-log instrumentation if the file does not
  artifacts:
    - path: apps/api/src/api/v1/db/session.py
      contains: create_engine
    - path: apps/api/src/api/v1/db/session.py
      contains: get_db
  key_links:
    - from: apps/api/src/api/v1/db/session.py
      to: apps/api/src/alembic/
      pattern: alembic
---

## objective

Close **OPS-01**: remove debug/agent instrumentation and **silent SQLite `create_all`** from the application database bootstrap; align operator docs and CONCERNS with **migration-only** discipline.

**Output:** Clean `session.py`, updated CONCERNS (debug/SQLite bullets), and explicit SQLite-first-run steps in install/setup docs.

## must_haves

1. **`session.py`** contains only `get_settings()`, `create_engine`, `sessionmaker`, and `get_db` — no `debug-0c9557.log`, no `sqlite_master` probe for auto-DDL, no `Base.metadata.create_all` in this module.
2. **`docs/installation.md`** (section SQLite / Opção C) states that after pointing `DATABASE_URL` to a file-based SQLite URL, the operator must run **`alembic upgrade head`** (from `apps/api` or documented working directory) before expecting tables.
3. **`docs/setup_database_guide.md`** adds or reinforces a short **“SQLite local / primeiro arranque”** bullet if missing: empty DB → migrations, not server auto-create.
4. **`.planning/codebase/CONCERNS.md`**: Remove or rewrite the bullet that lists **`config.py`** for `#region agent log` **if** `grep` shows no such region in `apps/api/src/api/v1/core/config.py`; update the SQLite/`create_all` bullet to **resolved** or remove once `session.py` is fixed.

## verification

- Run: `pytest tests/ -q --tb=short` (or at minimum `pytest tests/unit/test_deps.py tests/test_main.py -q --tb=short` if full suite is slow).
- Grep: `Select-String -Path apps/api/src/api/v1/db/session.py -Pattern "#region agent log|create_all|debug-0c9557"` returns **no** matches.
- Grep: `docs/installation.md` contains `alembic upgrade head` in or near the SQLite option section.

<tasks>

<task type="auto">
  <name>Task 1: Strip agent log and create_all from session.py</name>
  <read_first>
    - apps/api/src/api/v1/db/session.py
    - .planning/phases/04-operations-dependency-hygiene/04-CONTEXT.md
  </read_first>
  <files>
    apps/api/src/api/v1/db/session.py
  </files>
  <action>
    - Delete the entire block from `# #region agent log` through `# #endregion` (lines 14–41 inclusive in the current file).
    - Remove imports that become unused: `os`, and `text` from `sqlalchemy` if neither is used after the deletion. Keep `create_engine`, `sessionmaker`, `Session`, `Generator`, `get_settings`.
    - Ensure the module still: reads `settings.database_url`, sets `engine = create_engine(settings.database_url, pool_pre_ping=True)`, defines `SessionLocal = sessionmaker(...)`, and defines `get_db()` unchanged in behavior (yield session, close in finally).
    - Do **not** add replacement `create_all` anywhere in this file.
  </action>
  <acceptance_criteria>
    - File `apps/api/src/api/v1/db/session.py` does not contain the substring `#region agent log`.
    - Same file does not contain `create_all` or `debug-0c9557` or `sqlite_master`.
    - Same file still contains `engine = create_engine` and `def get_db(`.
  </acceptance_criteria>
  <verify>
    - Select-String -Path apps/api/src/api/v1/db/session.py -Pattern "create_engine|get_db|SessionLocal"
  </verify>
  <done>
    Application DB session module has no side-effect debug I/O or implicit DDL.
  </done>
</task>

<task type="auto">
  <name>Task 2: CONCERNS — align debug and SQLite bullets</name>
  <read_first>
    - .planning/codebase/CONCERNS.md
    - apps/api/src/api/v1/core/config.py
    - apps/api/src/api/v1/db/session.py
  </read_first>
  <files>
    .planning/codebase/CONCERNS.md
  </files>
  <action>
    - In **Tech Debt**, update the **debug/agent instrumentation** bullet: remove reference to `apps/api/src/api/v1/core/config.py` **unless** that file actually contains `#region agent log` or `debug-0c9557` (verify with grep first; current codebase has none in config).
    - Update or remove the **SQLite bootstrap via create_all** bullet to state **resolved** once Task 1 is merged, or delete the bullet and add a one-line note under a **Resolved** subheading pointing to Phase 4 / Alembic-only bootstrap.
    - Do not remove unrelated concerns (register rate limit, security items, etc.).
  </action>
  <acceptance_criteria>
    - `Select-String -Path .planning/codebase/CONCERNS.md -Pattern "config.py.*agent log|config.py.*debug-0c9557"` returns no matches **if** config.py has no such code (expected).
    - CONCERNS either no longer describes `create_all` in `session.py` as open debt, or marks it resolved with a short rationale.
  </acceptance_criteria>
  <verify>
    - Select-String -Path apps/api/src/api/v1/core/config.py -Pattern "agent log|debug-0c9557" -Quiet
  </verify>
  <done>
    Planning debt doc matches the codebase.
  </done>
</task>

<task type="auto">
  <name>Task 3: Docs — SQLite first run uses Alembic</name>
  <read_first>
    - docs/installation.md
    - docs/setup_database_guide.md
    - apps/api/src/alembic.ini
  </read_first>
  <files>
    docs/installation.md
    docs/setup_database_guide.md
  </files>
  <action>
    - In **`docs/installation.md`**, locate **Opção C: SQLite** (or equivalent). Add explicit steps: set `DATABASE_URL=sqlite:///./siscav_dev.db` (or project convention), **cd** to the directory where `alembic.ini` lives (`apps/api` if that is where Alembic is run today), run **`alembic upgrade head`**, then start the API. Remove or rewrite any sentence that implies tables are created **automatically** on first server start without migrations.
    - In **`docs/setup_database_guide.md`**, add a short subsection or bullet **“SQLite / desenvolvimento”** stating the same: schema comes from **Alembic**, not from the API import path.
    - Use Portuguese consistent with surrounding sections.
  </action>
  <acceptance_criteria>
    - `docs/installation.md` contains the literal substring `alembic upgrade head` in the SQLite-related section.
    - `docs/setup_database_guide.md` contains `alembic` and `SQLite` (case-insensitive) in the same paragraph or adjacent bullets describing first-time setup.
  </acceptance_criteria>
  <verify>
    - Select-String -Path docs/installation.md -Pattern "alembic upgrade head"
    - Select-String -Path docs/setup_database_guide.md -Pattern "SQLite|sqlite"
  </verify>
  <done>
    Operators are not misled about implicit create_all.
  </done>
</task>

</tasks>

## PLANNING COMPLETE

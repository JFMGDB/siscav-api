---
phase: 04-operations-dependency-hygiene
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - requirements.txt
  - requirements-dev.txt
  - pyproject.toml
  - .planning/codebase/CONCERNS.md
  - docs/installation.md
autonomous: true
requirements_addressed:
  - OPS-02
must_haves:
  truths:
    - Every direct runtime dependency line in requirements.txt uses `==` pins (including extras syntax e.g. `uvicorn[standard]==...`, `passlib[argon2]==...`, `python-jose[cryptography]==...`)
    - pyproject.toml `[project] dependencies` lists the same packages with matching pins
    - requirements-dev.txt pins pytest, pytest-cov, ruff, httpx after `-r requirements.txt`
    - `pytest` exits 0 after reinstall from pinned files
  artifacts:
    - path: requirements.txt
      contains: fastapi==
    - path: pyproject.toml
      contains: fastapi==
  key_links:
    - from: .github/workflows/ci.yml
      to: requirements-dev.txt
      pattern: requirements-dev
---

## objective

Close **OPS-02**: **reproducible installs** by pinning runtime and dev dependencies consistently with **CI** (Python **3.13**, `pip install -r requirements-dev.txt`).

**Output:** Pinned `requirements.txt`, pinned `requirements-dev.txt`, aligned `pyproject.toml`, CONCERNS “unpinned deps” resolved, and install docs stating CI’s install command.

## Pin set (concrete — use these exact versions)

From `pip freeze` after `pip install -r requirements.txt -r requirements-dev.txt` on Python 3.13:

| Package | Pin |
|---------|-----|
| fastapi | `fastapi==0.135.1` |
| uvicorn | `uvicorn[standard]==0.42.0` |
| sqlalchemy | `sqlalchemy==2.0.49` |
| pydantic | `pydantic==2.12.5` |
| alembic | `alembic==1.18.4` |
| psycopg2-binary | `psycopg2-binary==2.9.11` |
| passlib | `passlib[argon2]==1.7.4` |
| python-jose | `python-jose[cryptography]==3.5.0` |
| slowapi | `slowapi==0.1.9` |
| email-validator | `email-validator==2.3.0` |
| python-multipart | `python-multipart==0.0.20` |
| pytest | `pytest==9.0.2` |
| pytest-cov | `pytest-cov==7.1.0` |
| ruff | `ruff==0.14.6` |
| httpx | `httpx==0.28.1` |

If `pip install` fails on a platform (e.g. psycopg2 wheel), document the exception in a task note — otherwise keep pins as above.

## must_haves

1. CI command `pip install -r requirements-dev.txt` succeeds on a clean venv.
2. No unpinned bare package names remain in `requirements.txt` for the listed direct deps.
3. CONCERNS **Unpinned runtime dependencies** item marked resolved or removed.

## verification

- Run: `python -m pip install -r requirements-dev.txt` (fresh venv optional but recommended).
- Run: `pytest -q --tb=short` (full suite).
- Run: `ruff check .` and `ruff format --check .` (match CI).

<tasks>

<task type="auto">
  <name>Task 1: Pin requirements.txt</name>
  <read_first>
    - requirements.txt
    - .planning/phases/04-operations-dependency-hygiene/04-CONTEXT.md
  </read_first>
  <files>
    requirements.txt
  </files>
  <action>
    Replace unpinned lines with these exact lines (order may match logical grouping):
    ```
    fastapi==0.135.1
    uvicorn[standard]==0.42.0
    sqlalchemy==2.0.49
    pydantic==2.12.5
    alembic==1.18.4
    psycopg2-binary==2.9.11
    passlib[argon2]==1.7.4
    python-jose[cryptography]==3.5.0
    slowapi==0.1.9
    email-validator==2.3.0
    python-multipart==0.0.20
    ```
    Do not add packages not previously listed unless required for install (none expected).
  </action>
  <acceptance_criteria>
    - `Select-String -Path requirements.txt -Pattern "^fastapi==0.135.1$"` matches.
    - `Select-String -Path requirements.txt -Pattern "^[a-z]" | Where-Object { $_.Line -notmatch "==" }` returns no lines (every non-empty dependency line contains `==`).
  </acceptance_criteria>
  <verify>
    - Get-Content requirements.txt
  </verify>
  <done>
    Runtime deps are pinned.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pin requirements-dev.txt</name>
  <read_first>
    - requirements-dev.txt
    - requirements.txt
    - .github/workflows/ci.yml
  </read_first>
  <files>
    requirements-dev.txt
  </files>
  <action>
    - Keep first line `-r requirements.txt`.
    - Replace dev unpinned lines with:
      ```
      pytest==9.0.2
      pytest-cov==7.1.0
      ruff==0.14.6
      httpx==0.28.1
      ```
  </action>
  <acceptance_criteria>
    - File starts with `-r requirements.txt`.
    - Each of `pytest`, `pytest-cov`, `ruff`, `httpx` lines contains `==`.
  </acceptance_criteria>
  <verify>
    - Get-Content requirements-dev.txt
  </verify>
  <done>
    Dev deps match CI expectations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mirror pins in pyproject.toml</name>
  <read_first>
    - pyproject.toml
    - requirements.txt
    - requirements-dev.txt
  </read_first>
  <files>
    pyproject.toml
  </files>
  <action>
    - In `[project] dependencies`, replace each unpinned string with the **same** pinned spec as `requirements.txt` (including extras): `fastapi==0.135.1`, `uvicorn[standard]==0.42.0`, `sqlalchemy==2.0.49`, `pydantic==2.12.5`, `alembic==1.18.4`, `psycopg2-binary==2.9.11`, `passlib[argon2]==1.7.4`, `python-jose[cryptography]==3.5.0`, `slowapi==0.1.9`, `email-validator==2.3.0`, and add **`python-multipart==0.0.20`** (present in requirements.txt but missing from pyproject today — add it to `[project] dependencies` to avoid drift).
    - In `[project.optional-dependencies] dev`, pin: `pytest==9.0.2`, `pytest-cov==7.1.0`, `ruff==0.14.6`, `httpx==0.28.1`.
    - Preserve comments where possible without breaking TOML string validity.
  </action>
  <acceptance_criteria>
    - `pyproject.toml` contains `python-multipart==0.0.20` inside `[project]` dependencies array.
    - `pyproject.toml` contains `fastapi==0.135.1` and `sqlalchemy==2.0.49`.
    - Dev optional array contains `pytest==9.0.2`.
  </acceptance_criteria>
  <verify>
    - Select-String -Path pyproject.toml -Pattern "fastapi==|python-multipart==|pytest=="
  </verify>
  <done>
    pyproject and requirements stay in sync.
  </done>
</task>

<task type="auto">
  <name>Task 4: CONCERNS + installation doc note</name>
  <read_first>
    - .planning/codebase/CONCERNS.md
    - docs/installation.md
  </read_first>
  <files>
    .planning/codebase/CONCERNS.md
    docs/installation.md
  </files>
  <action>
    - In CONCERNS, resolve or delete the **Unpinned runtime dependencies** tech-debt bullet (OPS-02).
    - In `docs/installation.md` **Instalação da API** (or **Pré-requisitos** / venv section), add one sentence: dependencies are pinned in `requirements.txt`; CI installs with `pip install -r requirements-dev.txt` (Python 3.13). Portuguese wording consistent with the file.
  </action>
  <acceptance_criteria>
    - CONCERNS does not list open “Unpinned runtime dependencies” with `requirements.txt` as unfixed **or** explicitly marks it resolved in Phase 4.
    - `docs/installation.md` contains `requirements-dev.txt` or `requirements.txt` and `pip install` in the install instructions.
  </acceptance_criteria>
  <verify>
    - Select-String -Path docs/installation.md -Pattern "requirements"
  </verify>
  <done>
    Docs and debt list reflect pinned workflow.
  </done>
</task>

</tasks>

## PLANNING COMPLETE

---
phase: 04-operations-dependency-hygiene
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - apps/api/src/api/v1/crud/crud_user.py
  - apps/api/src/api/v1/crud/crud_access_log.py
  - apps/api/src/api/v1/crud/crud_authorized_plate.py
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/ARCHITECTURE.md
  - docs/development/coding-standards.md
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements_addressed:
  - OPS-03
must_haves:
  truths:
    - No Python file under `apps/api/src/api/v1/crud/` remains in the repo (directory removed)
    - No production/test code imports `apps.api.src.api.v1.crud`
    - Contributor docs describe repositories/controllers only for data access
    - CONCERNS deprecated crud item resolved
  artifacts:
    - path: apps/api/src/api/v1/repositories/user_repository.py
      contains: class
  key_links:
    - from: docs/development/coding-standards.md
      to: apps/api/src/api/v1/repositories/
      pattern: repositor
---

## objective

Close **OPS-03**: remove the deprecated **`crud/`** package and eliminate duplicate maintenance paths; align **STRUCTURE**, **ARCHITECTURE**, **coding-standards**, and **CONCERNS**.

## must_haves

1. Grep across `**/*.py` confirms zero imports from `crud` package paths before delete.
2. After delete, `pytest -q` still passes.
3. Documentation no longer instructs `from apps.api.src.api.v1.crud import crud_user`.

## verification

- `Get-ChildItem -Recurse apps/api/src/api/v1/crud -ErrorAction SilentlyContinue` returns nothing after execution.
- `rg "api\.v1\.crud|from apps\.api\.src\.api\.v1\.crud" --glob "*.py"` returns no matches (or only excludes none).

<tasks>

<task type="auto">
  <name>Task 1: Verify no imports; delete crud modules</name>
  <read_first>
    - apps/api/src/api/v1/crud/crud_user.py
    - apps/api/src/api/v1/repositories/user_repository.py
    - .planning/phases/04-operations-dependency-hygiene/04-CONTEXT.md
  </read_first>
  <files>
    apps/api/src/api/v1/crud/crud_user.py
    apps/api/src/api/v1/crud/crud_access_log.py
    apps/api/src/api/v1/crud/crud_authorized_plate.py
  </files>
  <action>
    - Run repo-wide search (ripgrep or IDE): patterns `api.v1.crud`, `apps.api.src.api.v1.crud`, `from apps.api.src.api.v1 import crud`, `v1.crud`. Scope: all `*.py` under repo root including `tests/`.
    - If **any** real import remains (not inside the crud files themselves), refactor that call site to use the appropriate **`repositories/`** or **`controllers/`** API before deleting.
    - Delete the three files under `apps/api/src/api/v1/crud/`. If an `__init__.py` exists in that folder, delete it too. Remove the empty `crud` directory.
    - Do not add compatibility re-export shims unless grep found external consumers (CONTEXT says none expected).
  </action>
  <acceptance_criteria>
    - Path `apps/api/src/api/v1/crud` does not exist or contains no `.py` files.
    - `grep` / `Select-String` over `*.py` files for `api.v1.crud` returns zero matches.
  </acceptance_criteria>
  <verify>
    - pytest tests/ -q --tb=short
  </verify>
  <done>
    Dead CRUD package removed from runtime tree.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update planning codebase docs (STRUCTURE, ARCHITECTURE)</name>
  <read_first>
    - .planning/codebase/STRUCTURE.md
    - .planning/codebase/ARCHITECTURE.md
  </read_first>
  <files>
    .planning/codebase/STRUCTURE.md
    .planning/codebase/ARCHITECTURE.md
  </files>
  <action>
    - **STRUCTURE.md**: Remove the `crud/` tree line or replace with a note that data access lives under `repositories/` and `controllers/` only.
    - **ARCHITECTURE.md**: Remove or rewrite the subsection describing deprecated `crud/` modules; state that **repositories + controllers** are the supported pattern.
  </action>
  <acceptance_criteria>
    - `STRUCTURE.md` does not list `crud/` as an active package path **or** explicitly says it was removed in Phase 4.
    - `ARCHITECTURE.md` does not reference `crud_user.py` / `crud_access_log.py` as current code locations.
  </acceptance_criteria>
  <verify>
    - Select-String -Path .planning/codebase/STRUCTURE.md -Pattern "crud/"
    - Select-String -Path .planning/codebase/ARCHITECTURE.md -Pattern "crud_"
  </verify>
  <done>
    Planning artifacts match repo layout.
  </done>
</task>

<task type="auto">
  <name>Task 3: coding-standards.md — remove crud examples</name>
  <read_first>
    - docs/development/coding-standards.md
    - apps/api/src/api/v1/repositories/user_repository.py
  </read_first>
  <files>
    docs/development/coding-standards.md
  </files>
  <action>
    - Find the section listing `crud_user.py` paths and the example `from apps.api.src.api.v1.crud import crud_user`.
    - Replace with an example using **`UserRepository`** (or equivalent) from `apps.api.src.api.v1.repositories.user_repository` — match actual class/function names in that file (e.g. instantiate repository or call static methods as the codebase does).
    - Remove file-path bullets that only existed to point at deleted crud files.
  </action>
  <acceptance_criteria>
    - `docs/development/coding-standards.md` does not contain `api.v1.crud` or `crud_user`.
    - Same file contains `repositories` and `user_repository` (case-insensitive) in the revised example.
  </acceptance_criteria>
  <verify>
    - Select-String -Path docs/development/coding-standards.md -Pattern "crud"
  </verify>
  <done>
    New contributors are not directed to deleted code.
  </done>
</task>

<task type="auto">
  <name>Task 4: CONCERNS — mark deprecated crud resolved</name>
  <read_first>
    - .planning/codebase/CONCERNS.md
  </read_first>
  <files>
    .planning/codebase/CONCERNS.md
  </files>
  <action>
    - Update the **Deprecated CRUD modules retained** bullet: mark **resolved** (Phase 4) with note that `crud/` was removed after import audit, or delete the bullet entirely.
  </action>
  <acceptance_criteria>
    - CONCERNS does not describe `crud_*.py` as still present **without** a resolved note.
  </acceptance_criteria>
  <verify>
    - Select-String -Path .planning/codebase/CONCERNS.md -Pattern "Deprecated CRUD|crud_user"
  </verify>
  <done>
    OPS-03 reflected in tech debt.
  </done>
</task>

</tasks>

## PLANNING COMPLETE

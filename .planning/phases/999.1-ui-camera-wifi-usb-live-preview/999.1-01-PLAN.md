---
phase: 999.1-ui-camera-wifi-usb-live-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/frontend/README.md
  - docs/frontend/camera-preview-nextjs.md
autonomous: true
requirements_addressed: []
must_haves:
  truths:
    - docs/frontend/camera-preview-nextjs.md exists and specifies Next.js TypeScript, USB (getUserMedia + video), Wi‑Fi (URL stream MJPEG/HLS/snapshot), UX states, and env NEXT_PUBLIC_SISCAV_API_URL
    - docs/frontend/README.md indexes the camera guide and states the UI lives in a separate Git repository
  artifacts:
    - path: docs/frontend/camera-preview-nextjs.md
      contains: getUserMedia
    - path: docs/frontend/README.md
      contains: Next.js
  key_links:
    - from: docs/frontend/README.md
      to: docs/frontend/camera-preview-nextjs.md
      pattern: camera-preview
---

## objective

Entregar neste repositório (**siscav-api**) a **especificação escrita** para implementar no **repositório Next.js + TypeScript** a fase 999.1: ligação **USB** e **Wi‑Fi (URL)** com **pré-visualização em tempo real**, alinhada a `.planning/phases/999.1-ui-camera-wifi-usb-live-preview/999.1-CONTEXT.md`.

**Purpose:** Executores humanos ou agentes no outro repo seguem o guia; este repo não contém código Next.

**Output:** `docs/frontend/README.md` + `docs/frontend/camera-preview-nextjs.md` completos e verificáveis por grep.

## must_haves

1. Guia menciona explicitamente **Client Components** / `'use client'` onde a API do browser é usada.
2. Dois fluxos de UI: **USB** e **URL de rede**; estados erro/permissão/stream ativo.
3. Referência a `docs/api/FRONTEND_INTEGRATION.md` para JWT quando houver chamadas à API (fora do scope estrito de preview).
4. **Fora de scope** do guia: OCR, upload `POST /api/v1/access_logs/` (só menção opcional como futuro).

## verification

- `grep -n "getUserMedia\\|MJPEG\\|HLS\\|NEXT_PUBLIC" docs/frontend/camera-preview-nextjs.md` produz hits.
- `docs/frontend/README.md` contém `camera-preview-nextjs.md` ou equivalente link textual.

<tasks>

<task type="auto">
  <name>Task 1: Índice docs/frontend</name>
  <read_first>
    - .planning/phases/999.1-ui-camera-wifi-usb-live-preview/999.1-CONTEXT.md
    - .planning/phases/999.1-ui-camera-wifi-usb-live-preview/999.1-RESEARCH.md
    - README.md
  </read_first>
  <files>
    docs/frontend/README.md
  </files>
  <action>
    Criar `docs/frontend/README.md` com:
    - Título e uma frase: UI operador **Next.js + TypeScript** vive em **repositório Git separado** deste.
    - Lista com link relativo Markdown para `./camera-preview-nextjs.md` (título: guia de pré-visualização USB / Wi‑Fi).
    - Referência a `docs/api/FRONTEND_INTEGRATION.md` para autenticação contra a API.
    - Referência a `apps/api/src/main.py` (secção CORS) — já permite `localhost:3000`.
  </action>
  <acceptance_criteria>
    - Ficheiro `docs/frontend/README.md` existe.
    - Contém a substring `camera-preview-nextjs.md`.
    - Contém `Next.js` e `TypeScript` (grep case-sensitive ou insensitive aceite).
  </acceptance_criteria>
  <verify>
    - Test-Path docs/frontend/README.md
  </verify>
  <done>
    Índice do frontend na documentação da API repo criado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Guia camera-preview-nextjs.md</name>
  <read_first>
    - .planning/phases/999.1-ui-camera-wifi-usb-live-preview/999.1-CONTEXT.md
    - .planning/phases/999.1-ui-camera-wifi-usb-live-preview/999.1-RESEARCH.md
    - docs/api/FRONTEND_INTEGRATION.md
    - apps/api/src/main.py
  </read_first>
  <files>
    docs/frontend/camera-preview-nextjs.md
  </files>
  <action>
    Criar `docs/frontend/camera-preview-nextjs.md` (Português ou EN consistente com resto de docs; aqui PT-PT alinhado ao projeto) com secções obrigatórias:

    1. **Objetivo** — preview tempo real; USB vs URL rede; fora de scope upload access_logs e OCR (bullet list).
    2. **Pré-requisitos** — Node 18+, Next com TS; variável `NEXT_PUBLIC_SISCAV_API_URL` exemplo `http://127.0.0.1:8000`; HTTPS em produção para getUserMedia.
    3. **Arquitetura** — diagrama ASCII ou bullets: browser ↔ câmara USB (getUserMedia); browser ↔ URL MJPEG/HLS; API SISCAV só para auth/outras features.
    4. **USB** — snippet conceitual (não precisa compilar aqui): `getUserMedia`, `<video autoPlay playsInline muted>`, `srcObject`, cleanup `getTracks().stop()`, lista de devices `enumerateDevices` após permissão.
    5. **Wi‑Fi / URL** — MJPEG em `<img>`; HLS com nota hls.js; snapshot polling como alternativa; aviso CORS/mixed content.
    6. **UX** — dois botões ou tabs: "Câmara USB" vs "URL na rede"; estados vazios texto sugerido (sem permissão, erro, a carregar).
    7. **Segurança** — não expor credenciais da câmara IP em logs; sanitizar URL; JWT só no cliente conforme FRONTEND_INTEGRATION.
    8. **Checklist de implementação** — lista numerada ≥ 8 itens verificáveis (ex.: criar rota `/camera`, componente client, testar Chrome desktop).

    Mínimo ~80 linhas úteis (não contar linhas em branco só).
  </action>
  <acceptance_criteria>
    - `docs/frontend/camera-preview-nextjs.md` existe e tem pelo menos 80 linhas (PowerShell: `(Get-Content ... | Measure-Object -Line).Lines -ge 80`).
    - Ficheiro contém literalmente `getUserMedia`.
    - Ficheiro contém `MJPEG` ou `HLS`.
    - Ficheiro contém `NEXT_PUBLIC_SISCAV_API_URL`.
    - Ficheiro contém `use client` ou `Client Component` (string).
  </acceptance_criteria>
  <verify>
    - Select-String -Path docs/frontend/camera-preview-nextjs.md -Pattern "getUserMedia" -Quiet
  </verify>
  <done>
    Guia de implementação Next.js para a fase 999.1 está no repo da API como fonte de verdade.
  </done>
</task>

</tasks>

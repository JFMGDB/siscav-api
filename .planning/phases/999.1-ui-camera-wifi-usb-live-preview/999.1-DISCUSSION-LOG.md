# Phase 999.1: Interface — câmara Wi‑Fi/USB - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.  
> Decisions are captured in `999.1-CONTEXT.md`.

**Date:** 2026-04-05  
**Phase:** 999.1-ui-camera-wifi-usb-live-preview  
**Mode:** yolo (config `.planning/config.json`) — sem seleção interativa de áreas; aplicados defaults recomendados para desbloquear research/plan.

**Areas covered (all):** Host da UI · USB/browser · Wi‑Fi/stream URL · UX de ligação · Fronteira com API SISCAV

---

## Host da UI

| Option | Description | Selected |
|--------|-------------|----------|
| Frontend separado ou futuro `apps/web` | Mantém API repo limpo; CORS já pensado para dev | ✓ |
| SPA dentro deste repo como produto principal | Acoplamento com release da API | |
| Só PoC estático no repo API | Aceitável só como exceção explícita no plano | |

**User's choice:** [yolo] Frontend separado ou `apps/web` futuro  
**Notes:** Alinhado com `PROJECT.md` (UI fora do core).

### Follow-up (utilizador)

| Option | Description | Selected |
|--------|-------------|----------|
| Repo separado + Next.js + TypeScript | Stack e localização confirmados | ✓ |

**User's choice:** Repositório **separado**; **Next.js** + **TypeScript**.  
**Notes:** Substitui a ambiguidade “ou `apps/web` no monorepo”; monorepo **não** é o alvo.

---

## USB / pré-visualização local

| Option | Description | Selected |
|--------|-------------|----------|
| getUserMedia + `<video>` | Padrão web; requer HTTPS/localhost | ✓ |
| Plugin nativo apenas | Fora do âmbito web-first | |

**User's choice:** [yolo] getUserMedia + video element  
**Notes:** Device picker quando há múltiplas câmaras.

---

## Wi‑Fi / câmara IP

| Option | Description | Selected |
|--------|-------------|----------|
| URL de stream (MJPEG / HLS / snapshot poll) no cliente | Sem proxy obrigatório na API no MVP | ✓ |
| Proxy de vídeo na FastAPI | Útil depois se CORS/rede o exigirem | |
| WebRTC completo com TURN | Complexidade alta — fase futura | |

**User's choice:** [yolo] Stream por URL no browser; sem proxy API no MVP  
**Notes:** Câmara deve ser alcançável pelo cliente.

---

## UX de ligação

| Option | Description | Selected |
|--------|-------------|----------|
| Dois fluxos: "USB" vs "URL rede" | Claro para operadores | ✓ |
| Um único wizard genérico | Menos explícito | |

**User's choice:** [yolo] Dois caminhos explícitos + estados de erro/carregamento  

---

## Fronteira com API (preview vs upload)

| Option | Description | Selected |
|--------|-------------|----------|
| Fase 999.1 = só ligar + ver preview | Escopo ROADMAP literal | ✓ |
| Incluir captura → POST access_logs | Nova capacidade — melhor fase seguinte | |

**User's choice:** [yolo] Só ligação + preview; upload integrado fica deferido  

---

## Claude's Discretion

- Dentro de Next.js: App vs Pages router, design system, bibliotecas HLS, microcopy, estilos.

## Deferred Ideas

- Captura + `POST /api/v1/access_logs/`, proxy de stream, WebRTC/TURN, gravação, OCR na UI.

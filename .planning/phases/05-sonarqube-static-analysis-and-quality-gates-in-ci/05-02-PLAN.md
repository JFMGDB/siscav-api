---
phase: 05-sonarqube-static-analysis-and-quality-gates-in-ci
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - .github/SONAR_SETUP.md
  - .github/README_CI.md
  - .planning/ROADMAP.md
autonomous: true
requirements_addressed:
  - SONAR-03
  - SONAR-04
must_haves:
  truths:
    - Operators can configure SonarCloud project, GitHub Secret `SONAR_TOKEN`, and optional `SONAR_HOST_URL` variable without reading Sonar marketing pages
    - Quality gate policy for this repo is explicit (initially non-blocking in CI; how to enable `qualitygate.wait`)
  artifacts:
    - path: .github/SONAR_SETUP.md
      contains: SONAR_TOKEN
  key_links:
    - from: .github/SONAR_SETUP.md
      to: sonar-project.properties
      pattern: sonar.projectKey
---

## objective

Document **SONAR-04** (setup) and **SONAR-03** (quality gate policy): SonarCloud vs self-hosted, required GitHub **Secrets/Variables**, alignment of `sonar.projectKey` / `sonar.organization`, fork limitations, and how to turn on **strict** quality gate later. Update **`.github/README_CI.md`** with a short pointer to the new doc. Update **`.planning/ROADMAP.md`** phase 5 plan checkboxes to reference `05-01-PLAN.md` and `05-02-PLAN.md`.

## must_haves

1. **`.github/SONAR_SETUP.md`** exists with sections: Prerequisites, SonarCloud onboarding (project key + org), GitHub `SONAR_TOKEN` secret, optional `SONAR_HOST_URL` repository variable for SonarQube Server, fork/PR caveats, Coverage file path (`coverage.xml`), Quality gate policy (non-blocking initially; add `sonar.qualitygate.wait=true` or action `args` when ready).
2. **`.github/README_CI.md`** contains a new bullet or short subsection linking to `SONAR_SETUP.md` (relative path from repo root: `.github/SONAR_SETUP.md`).
3. **`.planning/ROADMAP.md`** — under Phase 5, replace the single `TBD` plan bullet with links to the two new plan files and mark structure consistent with other phases (checkboxes unchecked until execute completes).

## verification

- Read: `SONAR_SETUP.md` is self-contained for a new maintainer.
- Grep: `README_CI.md` mentions `SONAR_SETUP`.

<tasks>

<task type="auto">
  <name>Task 1: Write .github/SONAR_SETUP.md</name>
  <read_first>
    - .github/workflows/ci.yml
    - sonar-project.properties
    - .planning/phases/05-sonarqube-static-analysis-and-quality-gates-in-ci/05-RESEARCH.md
    - .planning/phases/05-sonarqube-static-analysis-and-quality-gates-in-ci/05-CONTEXT.md
  </read_first>
  <files>
    .github/SONAR_SETUP.md
  </files>
  <action>
    Create `.github/SONAR_SETUP.md` in **Portuguese or English** matching the tone of `.github/README_CI.md` (repository uses PT in several docs — prefer **Portuguese** for consistency with `README_CI` title language).
    Include:
    1. **Objetivo** — análise estática + cobertura no SonarCloud/SonarQube.
    2. **SonarCloud** — criar projeto; copiar **Organization key** e **Project key**; colar em `sonar-project.properties` (`sonar.organization`, `sonar.projectKey`).
    3. **GitHub** — adicionar **Secret** `SONAR_TOKEN` (token de análise do SonarCloud ou token de projeto no servidor).
    4. **SonarQube Server** — definir **Variable** `SONAR_HOST_URL` com URL base (ex. `https://sonar.example.com`); não usar `sonarcloud.io`.
    5. **CI** — o workflow só corre o scan se `SONAR_TOKEN` estiver definido; até lá, CI permanece verde.
    6. **Cobertura** — `coverage.xml` na raiz após pytest; não apagar antes do scan.
    7. **Quality Gate** — fase inicial **sem** espera obrigatória pelo gate no CI; para falhar o job quando o gate falhar, documentar adição de `sonar.qualitygate.wait=true` em `sonar-project.properties` **ou** argumentos extra no passo `sonarqube-scan-action` (apontar para documentação Sonar).
    8. **Forks** — PRs de forks não têm secrets; comportamento esperado.
  </action>
  <acceptance_criteria>
    - File `.github/SONAR_SETUP.md` exists.
    - File contains the exact substring `SONAR_TOKEN`.
    - File contains the exact substring `sonar.projectKey` or reference to project key configuration in `sonar-project.properties`.
    - File contains the exact substring `qualitygate` (case insensitive) OR the phrase `Quality Gate` / `quality gate`.
  </acceptance_criteria>
  <verify>
    - Select-String -Path .github/SONAR_SETUP.md -Pattern "SONAR_TOKEN"
  </verify>
  <done>
    Setup is documented for operators.
  </done>
</task>

<task type="auto">
  <name>Task 2: Link from README_CI and refresh ROADMAP Phase 5 plans</name>
  <read_first>
    - .github/README_CI.md
    - .planning/ROADMAP.md
    - .planning/phases/05-sonarqube-static-analysis-and-quality-gates-in-ci/05-01-PLAN.md
  </read_first>
  <files>
    - .github/README_CI.md
    - .planning/ROADMAP.md
  </files>
  <action>
    - In `.github/README_CI.md`, after the pipeline steps list (or in a new **SonarQube** subsection), add 2–4 sentences and a markdown link to `./SONAR_SETUP.md` (or `SONAR_SETUP.md` relative to `.github/`).
    - In `.planning/ROADMAP.md`, under **Phase 5**, confirm **Plans:** lists two unchecked plan links to `05-01-PLAN.md` and `05-02-PLAN.md` (já pode estar atualizado pelo `/gsd-plan-phase`); se faltar, aplicar as mesmas linhas que em `.planning/ROADMAP.md` na versão pós-planeamento.
    - Garantir que não resta bullet `TBD — executar /gsd-plan-phase 5`.
  </action>
  <acceptance_criteria>
    - `.github/README_CI.md` contains substring `SONAR_SETUP.md`.
    - `.planning/ROADMAP.md` contains substring `05-01-PLAN.md` and `05-02-PLAN.md`.
    - `.planning/ROADMAP.md` Phase 5 section does not contain the substring `TBD — executar`.
  </acceptance_criteria>
  <verify>
    - Select-String -Path .github/README_CI.md -Pattern "SONAR_SETUP"
    - Select-String -Path .planning/ROADMAP.md -Pattern "05-01-PLAN|05-02-PLAN"
  </verify>
  <done>
    Discoverability and roadmap reflect the new plans.
  </done>
</task>

</tasks>

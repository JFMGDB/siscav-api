---
phase: 02-whitelist-access-log-behavior
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/api/v1/endpoints/whitelist.py
  - apps/api/src/api/v1/controllers/plate_controller.py
  - apps/api/src/api/v1/schemas/authorized_plate.py
  - docs/api/README.md
  - tests/integration/test_endpoints_whitelist.py
autonomous: true
requirements_addressed:
  - WL-01
must_haves:
  truths:
    - Authenticated CRUD on /api/v1/whitelist/ keeps strict BR plate validation and unique normalized_plate (409 on duplicate)
    - OpenAPI documents skip/limit and behavior of create/update for whitelist
    - docs/api/README.md states normalization rules and duplicate policy per D-04/D-05
  artifacts:
    - path: apps/api/src/api/v1/endpoints/whitelist.py
      contains: whitelist
    - path: docs/api/README.md
      contains: whitelist
  key_links:
    - from: apps/api/src/api/v1/endpoints/whitelist.py
      to: apps/api/src/api/v1/controllers/plate_controller.py
      pattern: PlateController
---

## objective

Prove and document **WL-01**: any authenticated user can **list, create, update, delete** authorized plates with **normalized plate matching** (strict Brazilian formats, case/format insensitive via existing `normalize_plate` / Pydantic), and **409** on duplicate `normalized_plate` — without relaxing D-04/D-05.

**Purpose:** Close gaps between behavior, OpenAPI, and operator docs for the whitelist surface.

**Output:** Clear API/docs contract + integration tests that fail if uniqueness or validation regresses.

## must_haves

1. **Create** rejects invalid plate format with **400** (existing validator); **Create/Update** that collides on `normalized_plate` returns **409** (current `PlateController` behavior preserved).
2. **List** supports `skip`/`limit` (1–100) as already implemented; OpenAPI `Query` descriptions remain accurate or improved.
3. **`docs/api/README.md`** includes a **Whitelist** subsection: formats accepted (Mercosul + legacy), normalization (strip non-alnum, upper), uniqueness on normalized value, typical error codes (400/409/404).

## verification

- Run: `pytest tests/integration/test_endpoints_whitelist.py -q --tb=short`
- Run: `pytest tests/unit/test_controllers_plate.py tests/unit/test_repositories_authorized_plate.py -q --tb=short`
- Grep: `docs/api/README.md` contains `whitelist` or `Whitelist` and `normaliz`

<tasks>

<task type="auto">
  <name>Task 1: Docstrings and OpenAPI for whitelist routes</name>
  <read_first>
    - apps/api/src/api/v1/endpoints/whitelist.py
    - apps/api/src/api/v1/controllers/plate_controller.py
    - apps/api/src/api/v1/schemas/authorized_plate.py
    - .planning/phases/02-whitelist-access-log-behavior/02-CONTEXT.md
  </read_first>
  <files>
    apps/api/src/api/v1/endpoints/whitelist.py
  </files>
  <action>
    - In `whitelist.py`, extend route docstrings (Portuguese ok) so they explicitly state:
      - **POST/PUT** use `AuthorizedPlateCreate` validation (Mercosul `ABC1D23` and legacy `ABC1234` per `validate_brazilian_plate`).
      - **Duplicate** same `normalized_plate` → **409 Conflict** with the existing detail string from `PlateController` (quote the exact `detail` substring used today, e.g. containing `duplicate` or `já existe` — copy from `plate_controller.py`).
      - **GET /** pagination: `skip` ≥ 0, `limit` 1–100, default `skip=0`, `limit=100`.
    - Ensure every `Query(...)` on `read_authorized_plates` has a non-empty `description=` (fill if any parameter lacks it).
    - Do **not** change URL paths or response models.
  </action>
  <acceptance_criteria>
    - `grep -n "409\\|Conflict\\|duplicate\\|já existe\\|normaliz" apps/api/src/api/v1/endpoints/whitelist.py` returns at least one hit for duplicate/409 intent in docstrings OR confirms docstring references validation error behavior.
    - `grep -n "skip\\|limit" apps/api/src/api/v1/endpoints/whitelist.py` shows Query parameters with descriptions on list route.
  </acceptance_criteria>
  <verify>
    - grep -n "read_authorized_plates" apps/api/src/api/v1/endpoints/whitelist.py
    - grep -n "Query(" apps/api/src/api/v1/endpoints/whitelist.py
  </verify>
  <done>
    Whitelist route docstrings and Query descriptions document validation, duplicates, and pagination.
  </done>
</task>

<task type="auto">
  <name>Task 2: Operator docs — Whitelist subsection</name>
  <read_first>
    - docs/api/README.md
    - .planning/phases/02-whitelist-access-log-behavior/02-CONTEXT.md
  </read_first>
  <files>
    docs/api/README.md
  </files>
  <action>
    - Add a section **`## Whitelist (placas autorizadas)`** (or equivalent Portuguese heading consistent with the file) that states:
      - Endpoints base path `/api/v1/whitelist/` and JWT requirement (`Authorization: Bearer`).
      - **Normalization:** `normalized_plate` is unique; user may send plate with hyphens; server normalizes for matching (reference behavior, not implementation detail dump).
      - **Formats:** Brazilian Mercosul and old 3 letters + 4 digits (align wording with `validate_brazilian_plate` docstring or schema error text).
      - **Errors:** 400 invalid format, 409 duplicate normalized plate, 404 unknown id on get/update/delete.
    - Place the section near other domain sections (after auth or before access logs if that section exists).
  </action>
  <acceptance_criteria>
    - `docs/api/README.md` contains the literal substring `Whitelist` or `whitelist`.
    - Same file contains `409` and `normaliz` (case-insensitive grep) OR Portuguese equivalent for duplicate uniqueness.
  </acceptance_criteria>
  <verify>
    - Select-String -Path docs/api/README.md -Pattern "Whitelist|whitelist" -Quiet; Select-String -Path docs/api/README.md -Pattern "409|normaliz" -Quiet
  </verify>
  <done>
    README documents whitelist rules for operators.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration tests for WL-01 edge cases</name>
  <read_first>
    - tests/integration/test_endpoints_whitelist.py
    - tests/conftest.py
    - apps/api/src/api/v1/controllers/plate_controller.py
  </read_first>
  <files>
    tests/integration/test_endpoints_whitelist.py
  </files>
  <action>
    - Add tests (or extend existing) in `test_endpoints_whitelist.py`:
      1. **Duplicate create:** POST two plates that normalize to the same `normalized_plate` (e.g. same `ABC-1234` twice) → second response **409**.
      2. **Invalid format:** POST body with plate failing `validate_brazilian_plate` (e.g. `AB-1` or `ABC12` — must trigger **400**).
      3. **List pagination:** With `auth_token`, create ≥2 plates via API, then `GET /api/v1/whitelist/?skip=0&limit=1` returns **200** and `len(response.json()) == 1`.
    - Reuse fixtures `client`, `auth_token`, `db_session` from `tests/conftest.py` as in existing tests in that file.
  </action>
  <acceptance_criteria>
    - File `tests/integration/test_endpoints_whitelist.py` contains `409` in an assertion or status check for duplicate scenario.
    - Same file contains `400` assertion for invalid plate test.
    - `pytest tests/integration/test_endpoints_whitelist.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    - pytest tests/integration/test_endpoints_whitelist.py -q --tb=short
  </verify>
  <done>
    Integration tests cover duplicate, invalid format, and list limit.
  </done>
</task>

</tasks>

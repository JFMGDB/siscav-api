---
phase: 02-whitelist-access-log-behavior
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/api/v1/endpoints/access_logs.py
  - apps/api/src/api/v1/repositories/access_log_repository.py
  - apps/api/src/api/v1/schemas/access_log.py
  - tests/integration/test_endpoints_access_logs.py
autonomous: true
requirements_addressed:
  - LOG-01
  - LOG-02
must_haves:
  truths:
    - POST ingest (with X-Device-Key) persists AccessLog fields matching AccessLogRead (LOG-01)
    - GET /api/v1/access_logs/ supports skip, limit, plate, status, start_date, end_date and returns newest-first order (LOG-02 / D-06)
  artifacts:
    - path: apps/api/src/api/v1/repositories/access_log_repository.py
      contains: order_by
    - path: tests/integration/test_endpoints_access_logs.py
      contains: start_date
  key_links:
    - from: apps/api/src/api/v1/endpoints/access_logs.py
      to: apps/api/src/api/v1/controllers/access_log_controller.py
      pattern: get_all
---

## objective

Verify and lock **LOG-01** (ingest persistence + schema) and **LOG-02** (list + filters + pagination per D-06): device-keyed **POST** persists `plate_string_detected`, `timestamp`, `status`, `image_storage_key`, optional `authorized_plate_id`; **GET /** applies `skip`, `limit`, optional `plate`, `status`, `start_date`, `end_date` and returns rows ordered by **`timestamp DESC`**.

**Purpose:** Regression-proof the audit list contract and ingest payload without changing Phase 1 auth.

**Output:** Repository hygiene (if needed), route/OpenAPI clarity, and integration tests that fail if ordering or filters break.

## must_haves

1. **Ingest** (with valid `X-Device-Key` and env from `tests/conftest.py`) returns JSON including `id`, `timestamp`, `plate_string_detected`, `status`, `image_storage_key`, and `authorized_plate_id` when plate is on whitelist (null when denied) — assert in tests.
2. **List** integration test proves **descending timestamp** order when multiple logs exist.
3. **List** integration test proves **`start_date` / `end_date`** exclude out-of-range rows (use ISO 8601 query values consistent with FastAPI parsing).

## verification

- Run: `pytest tests/integration/test_endpoints_access_logs.py -q --tb=short`
- Run: `pytest tests/test_access_logs.py -q --tb=short`
- Grep: `order_by(AccessLog.timestamp.desc()` in `access_log_repository.py`

<tasks>

<task type="auto">
  <name>Task 1: Repository cleanup and ordering guarantee</name>
  <read_first>
    - apps/api/src/api/v1/repositories/access_log_repository.py
    - apps/api/src/api/v1/controllers/access_log_controller.py
  </read_first>
  <files>
    apps/api/src/api/v1/repositories/access_log_repository.py
  </files>
  <action>
    - Remove **duplicate** top-level imports in `access_log_repository.py` (file currently repeats `from datetime import datetime` and `from typing import Optional` twice at lines 1–8 — keep a single import block).
    - Confirm `get_all` ends with `query = query.order_by(AccessLog.timestamp.desc()).offset(skip).limit(limit)` — do not change ordering semantics.
    - If `count()` exists, ensure it uses the same filter conditions as `get_all` (no code change if already aligned; if misaligned, fix so count matches filtered list).
  </action>
  <acceptance_criteria>
    - `access_log_repository.py` contains exactly **one** line `from datetime import datetime` at the top of the file (grep count 1 for that import at module level).
    - `grep -n "order_by(AccessLog.timestamp.desc" apps/api/src/api/v1/repositories/access_log_repository.py` finds the ordering line.
  </acceptance_criteria>
  <verify>
    - grep -n "from datetime import datetime" apps/api/src/api/v1/repositories/access_log_repository.py
    - grep -n "order_by" apps/api/src/api/v1/repositories/access_log_repository.py
  </verify>
  <done>
    Repository has no duplicate imports; newest-first order preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: OpenAPI and docstrings for list + ingest</name>
  <read_first>
    - apps/api/src/api/v1/endpoints/access_logs.py
    - apps/api/src/api/v1/schemas/access_log.py
    - .planning/phases/02-whitelist-access-log-behavior/02-CONTEXT.md
  </read_first>
  <files>
    apps/api/src/api/v1/endpoints/access_logs.py
    apps/api/src/api/v1/schemas/access_log.py
  </files>
  <action>
    - On `list_access_logs`, ensure each `Query` parameter includes a `description=` mentioning:
      - `plate`: partial, case-insensitive match on `plate_string_detected` (per repository `ilike`).
      - `start_date` / `end_date`: inclusive bounds on `timestamp` (ISO 8601).
      - Default sort: **most recent first** (`timestamp DESC`).
    - On `create_access_log` docstring, add one line: response body fields match **`AccessLogRead`** (`id`, `timestamp`, `plate_string_detected`, `status`, `image_storage_key`, `authorized_plate_id`).
    - Optionally add `json_schema_extra` or Field descriptions on `AccessLogRead` only if needed for OpenAPI clarity — **do not** rename fields.
  </action>
  <acceptance_criteria>
    - `grep -n "timestamp DESC\\|mais recente\\|recente primeiro" apps/api/src/api/v1/endpoints/access_logs.py` matches docstring or comment on list route.
    - `grep -n "AccessLogRead" apps/api/src/api/v1/endpoints/access_logs.py` appears in `create_access_log` docstring or nearby.
  </acceptance_criteria>
  <verify>
    - grep -n "list_access_logs" apps/api/src/api/v1/endpoints/access_logs.py
  </verify>
  <done>
    List and create routes document filter semantics and response shape.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration tests — ordering, date filters, ingest fields</name>
  <read_first>
    - tests/integration/test_endpoints_access_logs.py
    - tests/conftest.py
    - apps/api/src/api/v1/repositories/access_log_repository.py
  </read_first>
  <files>
    tests/integration/test_endpoints_access_logs.py
  </files>
  <action>
    - Add tests using `client`, `auth_token`, `db_session`, `TEST_DEVICE_INGEST_KEY` as in existing file:
      1. **Ordering:** Create **two** access logs via `POST /api/v1/access_logs/` (device key header) with different plates (or same denied flow) such that timestamps differ — e.g. insert second after small sleep or use repository with explicit times if tests use DB directly. Then `GET /api/v1/access_logs/?limit=10` with Bearer token: assert **first** element `timestamp` ≥ second (descending). If sleep is flaky, use `AccessLogRepository.create` via `db_session` with explicit `timestamp` values then commit, then GET list.
      2. **Date filter:** Create logs with known timestamps (via repository in `db_session`) spanning two days; call `GET /api/v1/access_logs/?start_date=...&end_date=...` (UTC ISO strings) and assert only in-range rows returned.
      3. **Ingest fields:** Extend or add assertion on existing authorized ingest test: response JSON has non-null `authorized_plate_id` when whitelist has plate; `image_storage_key` ends with expected extension.
    - Keep tests deterministic (prefer repository + commit for timestamp control over `time.sleep` when possible).
  </action>
  <acceptance_criteria>
    - `tests/integration/test_endpoints_access_logs.py` references `start_date` or `end_date` in a test function name or URL.
    - `pytest tests/integration/test_endpoints_access_logs.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    - pytest tests/integration/test_endpoints_access_logs.py -q --tb=short
  </verify>
  <done>
    Integration tests cover ordering, date bounds, and ingest payload fields.
  </done>
</task>

</tasks>
